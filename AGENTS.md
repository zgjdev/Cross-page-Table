# 项目 Agent 约定

本文件适用于仓库根目录及其所有子目录，是 Codex 和其他编码 Agent 的项目级约定。

## 范围与安全

- 服务器项目根目录为 `/data01/public/zhengguojie/paper`。
- 未经用户在当前对话中明确同意，不得删除、移动、截断或覆盖服务器数据。数据包括压缩包、解压数据、标准化数据、检查点、日志和生成结果。
- 在服务器上，未经用户明确授权，不得读取或修改 `/data01/public/zhengguojie/paper` 之外的任何文件。尤其不得检查其他用户目录、`/root` 或共享工作区。
- 服务器已有数据视为权威且必须持久保留。同步命令必须排除 `data/`、`artifacts/`、`outputs/` 和 `logs/`，除非用户明确批准特定例外。
- 不得在服务器执行宽范围 `rm -rf`、`git reset --hard` 或 `git checkout --` 等破坏性命令。
- 不得暴露、提交、上传或打印 API key、密码、token 或其他私密凭证。凭证必须保存在仓库之外。
- 代码先在本地修改。服务器命令仅用于用户确认后的验证、预处理、实验和训练。

## 文档语言

- 项目新增的研究文档、交接文档、实验记录、计划、说明和 Agent 反馈默认使用中文。
- 代码、命令、路径、配置键、函数名、数据字段、指标名和论文/模型的官方名称保留英文原文，以保证可执行性和术语准确性。
- 只有在用户明确要求英文，或目标文档本身是英文版本时，才使用英文正文。
- 新增双语文档时，中文版本是默认解释和执行依据；英文版本必须与中文版本保持一致。

## 研究目标

- 项目目标是形成可投稿高质量计算机视觉会议或期刊的研究贡献，不得偏移为无关的工程演示、通用 OCR 系统或仅做续表分类的项目。
- 核心研究问题是文档级跨页表格恢复，重点包括列对应、重复表头、跨页拆分行/单元格和约束重建。
- 主基准是固定 revision `aa575e798cb00a296925e2086addb3e3fd9a1903` 的 PubTables-v2 Full Documents。
- 必须分开报告 PDF-text-assisted 和 image-only 两条评测赛道，不得把不同输入条件的分数直接比较。

## 证据与科研严谨性

- 每个实质性研究判断都必须有依据：优先使用原始论文、官方数据集/模型文档、项目已有研究报告或已保存的实验记录，并注明来源或路径。
- 不得猜测数据字段、标签语义、模型能力、指标或实验结果。未知内容标记为“待核验”，并设计核验步骤。
- 在研究文档中区分“已提供”“推断”和“待核验”证据。
- 没有命令输出、日志或保存的产物证明时，不得声称下载、审计、实验或训练已经完成。
- 不得用 test split 调整规则、超参数或提示词。测试集只用于最终评估。
- 所有划分和泄漏检查必须按文档进行。同一文档的页面或片段不得跨越 train、validation 和 test。
- PubTables-v2 官方标签只能在页面对续接关系上称为官方标签。通过启发式得到的列对应、重复表头和拆分行标签必须称为派生标签或弱标签，并记录置信度和审计状态。

## 工作流要求

- 给用户服务器命令前，说明当前阶段、目的、输入、输出、是否下载数据、是否修改原始数据、是否使用 GPU 以及预期结果。
- 数据生成或模型训练前，优先执行小范围、可逆、只读检查。
- 每个实验都要保留数据版本、命令、环境、随机种子、配置和输出路径。
- 启动 GPU 任务前检查可用显存和正在运行的进程。不得终止其他用户进程，也不得在没有证据时假设 GPU 空闲。
- 在跨页关系样本、派生标签审计、泄漏检查和简单基线通过前，不得开始大规模模型训练。
- 原始压缩包、解压数据、标准化数据和生成产物必须分开保存。不得原地修改原始输入。

## 进度记录与工作留痕

- 不得只依赖当前对话上下文保存项目状态。项目进度、实验事实、重要判断和下一步计划必须及时写入仓库文档，以便在上下文压缩、任务中断或 Agent 更换后恢复工作，并为后续论文写作提供可核验依据。
- `docs/research-progress.zh-CN.md` 作为项目级进度索引；若文件尚不存在，应在首次产生实质性进展时创建。详细实验记录写入 `docs/experiments/` 下按日期和任务命名的中文文档，并由进度索引链接到对应记录、日志和产物。
- 遇到以下任一情况必须更新进度文档：完成一个可验收阶段；启动长时间或后台任务；任务完成、失败、中断或方案改变；获得新的正式指标或重要负结果；发现会改变既有结论的历史证据；准备结束当前工作轮次，或判断对话可能被压缩之前。
- 长时间任务启动时至少记录任务标识、启动时间、目的、输入范围、执行命令或脚本、环境、PID/日志路径、输出目录和预期完成条件；任务结束后补记结束状态、覆盖数量、错误统计、指标、产物校验和已知限制。
- 每条正式实验记录至少包含：日期与时区、研究问题、数据集名称与 revision、split 和样本数、输入筛选规则、模型与权重 revision、输入赛道（`PDF-text-assisted` 或 `image-only`）、代码 commit、环境与关键依赖、GPU、完整命令、随机种子、输出和日志路径、覆盖/重复/缺失检查、主要指标、错误与失败案例、结论边界及下一步。
- 记录必须明确区分“计划中”“运行中”“已完成”“失败/中断”“待核验”，并区分“已提供”“推断”和“待核验”证据。没有日志、命令输出或保存产物时，不得把工作记为已完成；不得为补齐记录而编造历史命令、环境、时间或指标。
- 对历史结果进行重新核验时，应保留原始结果不变，并追加记录核验日期、证据路径、协议差异和结论修正。例如必须说明是否使用真值筛页、是否覆盖无表页、是否执行跨页合并，以及旧指标能否与当前严格协议直接比较。
- 每次恢复项目工作时，必须先阅读本文件、`docs/research-progress.zh-CN.md`（如存在）、研究交接文档和与当前任务相关的最新实验记录，再决定下一步；发现文档与服务器产物不一致时，以可核验产物为依据并立即补充勘误记录。
- 进度更新应简洁但可复现，不要求记录无实质影响的每条只读命令；任何会影响论文主张、实验复现、公平比较或数据安全的信息均不得省略。

## 当前项目参考文档

- 研究交接和当前状态：`docs/research-handoff-2026-08-26.zh-CN.md`
- 研究执行计划：`docs/table-extraction-research-execution-plan.md`
- 文献与新颖性报告：`docs/reports/` 和 `docs/literature/`
- 数据集清单：`docs/reports/pubtables-v2-inventory.zh-CN.md`


## 服务器 Codex 自主执行

当用户通过服务器上的 `codex-paper` 启动 Codex CLI 时，允许 Codex 在当前项目目录内自主完成：

- 修改 `src/`、`scripts/`、`tests/`、`configs/` 和 `docs/`；
- 下载模型到 `models/`；
- 创建新的 `artifacts/`、`outputs/` 和 `logs/` 子目录；
- 执行数据检查、预处理、测试和实验；
- 等待长任务完成并检查日志；
- 在运行 GPU 任务前检查 GPU 状态和已有进程。

硬性限制：

- 不得删除、移动、截断或覆盖已有的 `data/` 文件；
- 不得删除或覆盖已有的标准化数据、实验结果、日志和检查点；
- 新实验必须使用新的带时间戳的输出目录；
- 不得读取或修改项目根目录之外的服务器文件；
- 不得访问其他用户目录、`/root` 或共享工作区；
- 不得终止其他用户的进程；
- 所有研究判断必须有论文、官方文档、命令输出或实验记录依据。
